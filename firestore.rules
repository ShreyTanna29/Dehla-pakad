rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP open rules for migration testing — tighten after profile/coins work.
    // Replace with the signed-in owner rules in firestore.rules.production when ready.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
